require 'test_helper'

class AdminSeesDashboardTest < ActionDispatch::IntegrationTest
  test "user needs credentials to access admin page" do
    skip
  end

  test "admin can navigate to admin page from /newsandevents with correct credentials" do
    skip
  end

  test "user cannot access admin page with wrong credentials" do
    skip
  end
end
